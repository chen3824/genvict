---
title: 跑通三模型，砍掉长任务：数据标注平台第二周 v0.2
summary: 不聊 AI，搞点真的。第二周，6 个检测模型接进标注工具，三模型 pipeline 跑通，批量标注不用守着点了。同一周的评审会上，砍掉了长任务断点续跑、数据管理和 QC 流程——不是做不了，是现在不该做。
tags: CASE.002 · MVP
---

![v0.2 头图：跑通与砍掉（建议尺寸 900×383，公众号头图 2.25:1）](imgs/00-cover.png)

> **TL;DR · 一分钟看懂**
> - ✅ 跑通 4 件：6 个模型接入 / 三模型自动 pipeline / 批量处理 / 重复框去重
> - ✂️ 砍掉 3 件：长任务断点续跑 / 数据管理 / QC 流程
> - 📊 标注时间预计**省下 60%**（估算，待中秋后实测）

不聊 AI，搞点真的。

---

## 场景：群里一张图，评审会两个方向

第二周刚开头，群里就热闹了。

8 月 26 号上午，算法同学小金往「AI 自动化算法数据治理平台」群里甩了张截图：监控画面里一辆银色车，系统自己把「输入图片 → 车头 → 车牌 → 字符」三步接成一条线，从头跑到尾。

**没配一句解释。图一发，群里都懂了。**

![小金在群里贴出 pipeline 截图（8/26）](imgs/01-chat.png)

两天后的评审会，话题掉了个头。那天一半时间没在聊「还能加什么」，在聊「哪些先别做」。

---

## 目标：图丢进去，结果自己出来

> 从 v0.1 的「单模型，一张张点」，变成「多模型自动 pipeline + 批量处理」。

说白了就一句：图丢进去，带标注的结果吐出来，中间别让人一直点鼠标。一摞图也一样，一起跑完。

![v0.1→v0.2 升级示意：从单模型单张到多模型 pipeline + 批量处理](imgs/04-upgrade.png)

---

## 这周跑通的事 · 4 件

### 1️⃣ 6 个检测模型一起接进来

高速小目标、城市枪机、城市鱼眼、收费站低位车头、车牌、车牌字符 —— v0.1 那两个照常用着，这版是把**现役检测模型统一接进来**，不用再手动挑模型文件。

### 2️⃣ 三模型自动 pipeline

> 车头检测 → 自动裁出车辆区域 → 车牌检测 → 裁出车牌 → 字符检测 → 坐标再映射回原图

一条线自己往下跑，**人不用守**。

![三模型 pipeline 示意：车头检测 → 车牌检测 → 字符识别](imgs/02-pipeline.png)

真实跑起来是这个界面——一张监控图进去，检测结果直接标在上面：

![v0.2 三模型 pipeline 演示](imgs/02-pipeline-demo.png)

### 3️⃣ 批量处理

打开图片目录，选好模型或 pipeline，点自动运行，剩下的图**一张张自己跑完**，不用守着。

![多模型接入 + 一摞图批量处理示意](imgs/03-multimodel-batch.png)

真实的模型列表长这样，要用的模型在菜单里直接选，选完点自动运行：

![v0.2 多模型接入与批量处理演示](imgs/03-multimodel-batch-demo.png)

### 4️⃣ 重复框自动去重

两个框重合面积到 **95%**，就当成同一个目标，只留置信度高的那个。

---

## 📊 最有感的那个数字

> **v0.2 这版，标注时间大概能省下 60%。**

![v0.2 阶段标注时间预估节省约 60%](imgs/05-metrics.png)

> ⚠️ 先当估算看。真数据得等中秋前部门试用、问卷收上来才有——**提效百分比和使用人数，是那时候才收得齐的东西。**

---

## ✂️ 砍掉的事 · 3 件

8 月 28 号评审会，明确三件**先不做**：

> **✂️ 长任务断点续跑，暂不做**
> 原本想处理一周以上的长视频抽帧，算了笔账：**一周视频大概 1T，本地存不下也跑不动，得先有云端。** 留到第二阶段。

> **✂️ 数据管理，不做**
> 现在数据散在各人电脑里，版本号都对不上。平台最多帮忙导出数据集，**做不了追踪**。

> **✂️ QC 流程，不归平台**
> 人工审核是每个算法工程师自己的事，平台只管给预标注和工具链，**不负责攒 QC 经验去优化模型。**

**一句话：平台只做数据处理的前端链路——收集、筛选、处理、标注、增强、导出。模型训练和数据治理，不是这一轮的事。**

> 💡 **这些「不做」跟做出来的功能一样重要。** 功能清单每加一条，后面就多一份要维护的东西；现在砍掉的，都是以后不用还的债。

---

## 小坑 · 3 个

都不是惊天大坑，都是小事。但小事一件件堆着，就是真实进度——**说明我们是在跑，不是在写 PPT。**

* 6 个模型接进来了，**每轮跑完还得人看一遍**。置信度默认卡在 **0.25**，低于这个数的结果不显示——错框少了一些，也可能漏掉一些真目标
* 60% 这个数是估出来的，**稳定数据还没攒够**
* v0.1 那两个模型原样跑着没动，v0.2 这条三模型 pipeline 是另起的一条路，**两套现在并行，谁也没替谁**

---

## 哪些环节仍然需要人

| 环节 | 谁负责 |
| --- | --- |
| 提供三模型 pipeline 链路需求与验收标准 | 算法工程师 |
| 训练 / 维护车头、车牌、字符等检测模型 | 算法工程师 |
| 审核并修正 pipeline 输出（车框 / 车牌 / 字符）的质量 | 标注人员 |
| 批量任务的最终质量把关 | 算法工程师 |
| 判断哪些功能该砍，守住平台边界 | 共创官 + 算法工程师 |

---

## 下周 · 把分类模型接进来

> 把车辆特征（颜色、车型、方向）和车牌颜色接进来，pipeline 从三模型扩到四模型。

以前标注员得手动把车牌按颜色分到不同文件夹，以后图丢进去，pipeline 一次跑完，**漏检和手动分拣都能省掉**。

---

## 最后

本期实验到这里结束。

以后，我们会把每一次真实的 AI 实践留下来：

* ✅ **做成了**，记录
* ❌ **做砸了**，也记录
* 💡 **好玩的**，记录
* 🚀 **真正产生价值的**，更要记录

> 👉 如果你工作里也有一件**特别烦、特别重复、特别想甩给 AI** 的事情，欢迎来找我们。

---

*本文由 AI 协作完成，人负责事实与审核。*