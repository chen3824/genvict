# AGENTS.md — 本知识库的操作规范 (Schema)

本文档是 Andrej Karpathy 所称的 **schema 层**：它把 LLM（这里是 OpenCode）从"通用聊天机器人"变成"有纪律的 wiki 维护者"。

## 0. 这是什么

本 Vault 是一个 **LLM-managed Wiki**（Karpathy, "LLM Wiki" 模式）：
人类负责选材、探索、提问与审核；LLM 负责所有的写作、链接、分类与维护。
三层架构：

```
raw/       Layer 1 — 原始资料（不可变，唯一事实来源）
wiki/      Layer 2 — LLM 维护的 wiki（互链的 markdown 页面）
AGENTS.md  Layer 3 — schema（本文件，规定 wiki 如何运转）
```

核心原则：**wiki 是一个持久的、复利的产物 (compounding artifact)**。知识被"编译一次、持续维护"，而不是每次提问都重新推导。

## 1. 目标

构建一个**可长期维护、互相连接、可被 AI 使用的个人知识库**：

- 每新增一份资料，wiki 变得更丰富、联系更紧密，而不是更乱。
- 回答过的问题、做过的比较、发现的联系，都被**沉淀回 wiki**，不消失在对话里。
- wiki 允许被任意 LLM 工具和 grep/Dataview 等普通工具读取。
- 人类能随时用 Obsidian 浏览、用自然语言向 wiki 提问。

**不要**把"每篇文章总结成一篇 markdown"当作核心工作方式。摘要只是副产品；
核心是维护一个由页面、链接、综述构成的持续演化网络。

## 2. 原始资料与 wiki 的关系

- `raw/` 是**唯一事实来源 (source of truth)**。里面是未经加工的原始材料：
  网页剪藏、PDF、论文、仓库、图片等。
- `raw/` **不可变**：LLM 只能读取，**永不修改**任何 raw 文件。
- `wiki/` 由 LLM 完全拥有：创建页面、更新页面、维护交叉引用、保持一致性。
- 人类**很少直接写** wiki，主要读 wiki、选材、提问题、审核。
- Obsidian 是 IDE；LLM 是程序员；wiki 是代码库。

## 3. 新资料如何进入知识库 (Ingest)

标准流程（默认一次一份，人类参与审阅；也可批量，需声明）：

1. 资料先落入 `raw/`（剪藏、下载，或经你指示放置）。
2. LLM **先只读**该 raw 资料，完整理解，不急着写。
3. 与用户讨论关键要点：哪些值得强调、归到哪个主题。
4. 在 `wiki/` 写入该来源的**摘要页**。
5. 更新 `wiki/index.md`。
6. 更新相关的**实体页 / 概念页 / 综述页**（可能横跨 10–15 个页面）。
7. 发现新矛盾时，在相关页面标注，不强行调和。
8. 在 `wiki/log.md` 追加一条 `## [日期] ingest | 标题` 记录。

摄入后由人类抽查：摘要是否准确、改动是否符合预期，再进入下一条。

## 4. OpenCode 如何阅读和处理资料

- **先读索引再深入**：回答查询或处理任务前，先读 `wiki/index.md` 定位相关页面，再逐个深入，避免凭空猜测 wiki 里有什么。
- **检查日志**：改动前看 `wiki/log.md` 最近条目，了解哪些刚处理过，避免重复劳动。
- **保留来源追踪**：wiki 页面中的每个主张都应能追溯到 `raw/` 中的具体来源。
- **先搜索后创作**：写任何页面之前，先确认现有页面里没有重复内容（见 §7）。
- 涉及不熟悉的主题时，先看已有相关页面再决定新建还是更新。

## 5. 何时创建新的 Wiki 页面

- 一个**反复出现**的实体或概念在 wiki 中尚不存在自己的页面时。
- 一份 raw 资料需要独立的摘要页时。
- 一次查询产出的答案足够有价值，值得作为独立页面沉淀时（见 §8 Query）。
- 一项 lint 发现的"缺失页面"补建时。
- **不**为单条琐碎信息建页；单次提及不足以建页，先进已有页面。

## 6. 何时更新已有 Wiki 页面

- 新来源为已有实体/概念补充了信息时，更新对应页面并补交叉引用。
- 新来源**推翻或修正**旧主张时，更新页面并在 `log.md` 中记录。
- 页面中的链接、分类随内容演化需要调整时。
- lint 发现的失效链接、孤儿页、待办标注修复时。

**不要**重写已经正确的部分；只做增量修改。

## 7. 如何避免重复知识

- 新建页面**前**，先搜索 `wiki/`（用 `[[wikilink]]` 和文本搜索）确认页面不存在。
- 页面命名遵循**单一主题 = 单一页面**：同一实体/概念永远只有一个页面。
- 发现两个页面覆盖同一主题时：合并到更合适的那个，在 `log.md` 记录，并更新所有指向被合并页的链接。
- 摘要页只描述来源本身；概念/实体页是跨来源的综合。两者职责不同，不重复。

## 8. 如何建立页面之间的链接

- 使用 Obsidian **wikilink**：`[[页面名]]`。
- 链接用页面名而非文件路径，避免改名时链接断裂。
- 每次新建或更新页面时，**反向检查**：哪些已有页面应链接到本页？补上。
- 引用 raw 来源时，链接到对应的 wiki 摘要页，并在文本中注明来源（如 `来源：[[摘要页名]]`）。
- 目标：概念之间形成紧密网络；孤立的、无人链接的页面是坏迹象（lint 会检查）。

## 9. 如何处理不确定的信息

- **区分"事实"与"推断/综合"**：raw 中有的写为事实；跨来源的推理明确标注为综合。
- 标注不确定性：使用 `?`、`[待确认]`、`存疑` 等标记。
- **矛盾不强行调和**：不同来源冲突时，保留双方并各自标注来源，在页面中列出冲突点，而不是擅自判定谁对。
- 信息不足时明确说明缺口，可写入 lint 的"待研究问题"。
- 不得在 wiki 中编造 raw 里不存在的信息。所有主张必须有来源；无来源的内容要显式标注为"个人推断"。

## 10. 如何保留来源

- 每个 wiki 页面顶部使用 YAML frontmatter 记录元数据：

```yaml
---
title: ...
type: concept | entity | source | overview
tags: [...]
sources: [文件名]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

- 主张旁用 wikilink 指向 `[[来源摘要页]]`，最终指向 `raw/` 中的原件。
- 外部链接（URL）仅作为补充，不替代本地来源。
- `raw/` 中的原件本身不可删除、不可改写。

## 11. 如何进行增量更新

- 每次改动尽量**小而聚焦**：一次只做一件完整的事（一份资料、一个主题、一条查询）。
- 修改后**立即**同步 `index.md` 与 `log.md`，保持索引与日志与内容一致。
- 遵循 `log.md` 的固定前缀格式，保证日志可被 unix 工具解析。
- 不批量重构整个 wiki，除非用户明确要求并接受大规模改动。

## 12. 什么情况下不能修改已有知识

- 用户明确说"不要动"的页面或区域。
- **未经确认不删除或重写**他人的综合内容；重大改写前先与用户确认。
- raw 文件任何时候都不能改。
- 不要把用户手工写下的内容当作可随意改写的 LLM 产物；用户手写的内容优先级最高。
- 对一个页面反复进行无实质收益的格式化重排 = 噪声，禁止。

## 13. 如何检查自己的修改

每次修改后自查：

1. `log.md` 是否追加了记录？格式是否正确？
2. `index.md` 是否已同步（新增/更新页面都被列出）？
3. 新页面的链接是否被反向链接补齐？有无断链？
4. 每个新主张是否都能追溯到来源？
5. 是否无意中修改了 `raw/`、`.obsidian/`、`.gitignore`？（不允许）
6. 页面内容是否有明显矛盾或重复？

## 14. Git 操作边界

- 本 Vault 是 git 仓库，wiki 因此天然有版本历史，可回滚 LLM 的错误写入。
- **只有在用户明确要求时才执行** `git add` / `git commit` / `git push`。
- 默认工作方式：修改文件即可，不自动提交。提交时机由用户决定。
- 不修改 git 配置、不 force-push、不删除他人历史。

## 15. 三种核心操作速查

- **Ingest（摄入）**：见 §3。
- **Query（查询）**：读 `index.md` 定位 → 深入相关页面 → 综合答案并给出引用；
  有价值的答案作为新页面**写回 wiki** 并记入 `log.md`。
- **Lint（体检）**：周期性健康检查，检查矛盾、过时主张、孤儿页、
  提及但无页面的概念、缺失交叉引用、可补充的网络搜索缺口；
  产出"待研究问题与待找来源"清单，写入 `log.md`。

## 16. 与 Karpathy 原始方法的关系（溯源）

本规范的核心直接来自 Karpathy 的一手资料 —— "LLM Wiki" idea file
(https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)：

- 三层架构（raw / wiki / schema）—— 原始方法
- 三大操作（ingest / query / lint）—— 原始方法
- index.md 与 log.md 的双文件设计 —— 原始方法
- "wiki 是代码库，Obsidian 是 IDE" 的哲学 —— 原始方法
- schema 由人机**共同演化 (co-evolved)** —— 原始方法

针对 OpenCode + Obsidian 的工程化补充：
- 具体 YAML frontmatter 字段（type/tags/sources/created/updated）
- §4 的"先读 index、再看 log、先搜索后创作"的执行顺序
- §7 单页命名与合并规则的落地细节
- §12/§13/§14 针对自动化工具的护栏（哪些不能改、如何自查、Git 边界）

schema 与工程细节会随使用演化，但顶层模式保持简单。
